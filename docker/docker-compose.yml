version: '3.8'

services:
  speedtest:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: speedtest-app
    ports:
      - "8000:8000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped